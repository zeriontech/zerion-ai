# Empty by default. The package.json `overrides` block resolves the only
# peer-dep mismatch (web-zk-prover@2.0.1 declaring stale @umbra-privacy/sdk
# peer range). Snarkjs is auto-installed as a peer of web-zk-prover.
